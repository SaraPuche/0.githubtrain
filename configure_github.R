# Configure GIT

# We have to tell GIT who we are

library(usethis)
use_git_config(user.name= "SaraPuche", user.email= "sara.puche@uclm.es")

# if I currently working in a project:
library(usethis)
use_git()

# restart Rstudio